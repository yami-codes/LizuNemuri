import 'package:flutter/material.dart';
import 'package:lizunemu/screens/contents/favorites_content.dart';

/// Bottom tab: favorites grid (login-gated).
class FavoritesTabContent extends StatelessWidget {
  const FavoritesTabContent({super.key});

  @override
  Widget build(BuildContext context) => const FavoritesContent();
}
