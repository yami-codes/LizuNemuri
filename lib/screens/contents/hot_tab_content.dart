import 'package:flutter/material.dart';
import 'package:xuro/screens/contents/popular_content.dart';

/// Eara-style "Hot" tab — wraps the existing popular-works grid.
class HotTabContent extends StatelessWidget {
  const HotTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const PopularContent();
  }
}
