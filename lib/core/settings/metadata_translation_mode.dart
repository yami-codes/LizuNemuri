/// When metadata translation runs for lists and track names.
enum MetadataTranslationMode {
  /// Translate automatically after each page load.
  auto,

  /// Only translate when the user taps a translate action.
  manual,
}
