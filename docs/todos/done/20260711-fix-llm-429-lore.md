# Fix LLM 429 rate-limit handling for Work Lore

- **Created**: 2026-07-11
- **Owner**: mirai
- **Status**: done
- **Related Issue / PR**:

---

## 1. Goal

> Stop Work Lore from silently failing track passes on HTTP 429; retry with backoff and surface a clear rate-limit error to the UI.

## 2. Scope

**In scope:**
- `LlmClient` 429 retries (`Retry-After` / exponential backoff)
- Inter-track pacing in `WorkLoreService.generate`
- Rethrow rate-limit / auth / config errors from `_generateTrackPass`
- `WorkLoreViewModel` surfaces `LlmTranslationException.userMessage`
- Unit tests for 429 mapping + backoff

**Out of scope:**
- Provider-specific quota dashboards
- Changing free-tier model selection UX

## 3. Acceptance

- [x] 429 retries up to 5 times with backoff before failing
- [x] Track pass does not swallow `rateLimited` / auth / missing key
- [x] UI shows `Strings.llmErrorRateLimited` (or auth/config copy) on failure
- [x] 1.5s delay between sequential track LLM calls
- [x] `flutter analyze` / relevant tests pass

## 4. Steps

- [x] **Step 1**: `LlmClient` 429 retry + `rateLimitBackoffForTest`
- [x] **Step 2**: Inter-track delay + rethrow in `_generateTrackPass`
- [x] **Step 3**: ViewModel `userMessage` wiring
- [x] **Step 4**: Tests + analyze

## 5. Risks

- **Risk**: Long waits on sustained 429 (up to ~5 retries × Retry-After)
- **Rollback**: Revert client retry / track delay commits

## 6. Notes / Decision Log

> Free/OpenRouter models hammered by multi-track lore; gtx-style silent empty tracks were worse UX than a visible rate-limit message. Auth/config errors also rethrown so users aren't left with empty low-confidence tracks.

---

## ✅ Done

- Completed at: 2026-07-11 11:50
- Command run: `/init` (manual CLAUDE.md lore/LLM rate-limit note)
- CLAUDE.md update summary: Document LlmClient 429 retry + lore track-pass rethrow / inter-track pacing invariants.
- Related commit: (pending user commit)
